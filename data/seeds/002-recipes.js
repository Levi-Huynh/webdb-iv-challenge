
exports.seed = function(knex, Promise) {
  // Deletes ALL existing entries
  // return knex('recipes').truncate()
  //   .then(function () {
      // Inserts seed entries
      // return knex('recipes').insert([
      //   {name:'Mex Fajitas', dish_id:1, instructions:'Cook ground beef. Add ground beef to taco shell. Garnish with cheese and salse.'},
      //   {name:'Italian Pie', dish_id:2, instructions:'Cook sausage. Add sausage, cheese and tomato sauce to dough. Cook Pizza.'},
      //   {name:'Beef Patty', dish_id:3, instructions:'Cook ground beef patty. Add patty to bread buns. Garnish with cheese.'}
      // ]);
    //  });
};
